{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "type": "shell",
            "command": "mvn compile exec:java -Dexec.mainClass=ltd.thzs.bili.sprider.App",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}